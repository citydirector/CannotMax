# 日志增强和鲁棒性改进

## ✅ 已完成的改进

### 1. 详细的每回合日志

**修改文件**: `auto_fetch.py` - `fill_data()` 函数

**新增日志格式**:
```
📊 回合数据已写入 | 结果: L | 左: [ID3x2, ID15x1] | 右: [ID28x3, ID42x1]
```

**显示内容**:
- ✅ 对战结果（L/R）
- ✅ 左侧所有怪物及数量
- ✅ 右侧所有怪物及数量
- ✅ 确认数据已写入CSV

**好处**:
- 实时监控收集的数据是否正确
- 人眼可以快速验证识别准确性
- 方便调试和问题排查

---

### 2. 模型未加载时的警告

**修改文件**: `auto_fetch.py` - `recognize_and_predict()` 函数

**新增警告**:
```
⚠️ 模型未加载，跳过预测（current_prediction保持默认值0.5）
   如果是'从0开始收集数据'模式，这是正常的
   如果是'自动获取数据'且启用投资，预测将不准确！
```

**保护机制**:
- 设置默认预测值 0.5
- 明确告知用户当前状态
- 区分不同使用场景的影响

---

### 3. "从0收集数据"功能的鲁棒性增强

**修改文件**: `auto_collect_and_train.py` - `_run_workflow()` 函数

**新增提示**:
```
🎮 启动数据收集（固定选左）...
ℹ️ 提示: 此模式不使用模型预测，仅收集原始数据
...
🔒 策略: 固定观望（不投资），确保数据一致性
...
ℹ️ 模型未加载（已备份或不存在），这是正常的
   数据收集不依赖模型，可以正常进行
```

**鲁棒性保证**:

| 场景 | 是否影响数据收集 | 说明 |
|------|----------------|------|
| 模型文件不存在 | ❌ 不影响 | 只收集怪物识别数据，不用预测 |
| 模型维度不匹配 | ❌ 不影响 | 同上 |
| 模型加载失败 | ❌ 不影响 | AutoFetch会捕获异常并继续 |
| 旧模型被备份 | ❌ 不影响 | 这正是预期行为 |

**关键设计**:
1. `is_invest=False` - 固定不投资，不使用预测结果
2. `dummy_update_prediction` - 空回调，忽略预测值
3. 每局固定点击"本轮观望" - 不依赖模型决策
4. 只记录识别到的怪物数据 - 纯数据收集

---

## 🔍 鲁棒性分析

### "从0收集数据" vs "自动获取数据"

| 特性 | 从0收集数据 | 自动获取数据(投资) |
|------|------------|------------------|
| 依赖模型 | ❌ 不依赖 | ✅ 强依赖 |
| 模型错误的影响 | 无影响 | **严重**（错误预测→错误投资） |
| 数据质量 | ✅ 高（纯原始数据） | ⚠️ 可能混入错误决策 |
| 适用场景 | 建立新模型 | 日常使用已有模型 |

### 为什么"从0收集"是安全的？

```python
# 1. 固定不投资
is_invest=False

# 2. 每局都点"观望"
if self.is_invest:
    # 投资逻辑（不会执行）
else:
    self.adb_connector.click(relative_points[4])  # 本轮观望
    logger.info("本轮观望")

# 3. 预测结果不被使用
def dummy_update_prediction(pred):
    pass  # 空的回调，预测值被丢弃

# 4. 只保存识别结果
fill_data(battle_result, recognize_results, ...)
# battle_result 来自图像识别（左右饱和度对比）
# recognize_results 来自怪物模板匹配
# 两者都不依赖模型预测
```

---

## 📊 日志示例

### 正常运行时
```
2026-04-26 04:30:15 - auto_fetch - INFO - 匹配到状态: GameState.PRE_BATTLE
2026-04-26 04:30:16 - recognize - INFO - target: 2 matched_id: 24, confidence: 0.9661
2026-04-26 04:30:16 - recognize - INFO - OCR: text: '3', score: 0.99966
2026-04-26 04:30:17 - auto_fetch - INFO - 本轮观望
2026-04-26 04:30:22 - auto_fetch - INFO - 📊 回合数据已写入 | 结果: L | 左: [ID24x3] | 右: [ID39x1]
2026-04-26 04:30:22 - auto_fetch - INFO - 写入csv完成
```

### 模型未加载时（从0收集）
```
2026-04-26 04:30:15 - auto_collect - INFO - ℹ️ 模型未加载（已备份或不存在），这是正常的
2026-04-26 04:30:15 - auto_collect - INFO -    数据收集不依赖模型，可以正常进行
2026-04-26 04:30:17 - auto_fetch - WARNING - ⚠️ 模型未加载，跳过预测（current_prediction保持默认值0.5）
2026-04-26 04:30:17 - auto_fetch - WARNING -    如果是'从0开始收集数据'模式，这是正常的
2026-04-26 04:30:22 - auto_fetch - INFO - 📊 回合数据已写入 | 结果: R | 左: [ID15x2] | 右: [ID8x1, ID22x3]
```

---

## ✅ 结论

1. **日志增强** - 现在可以清楚看到每回合写入的数据
2. **鲁棒性保证** - "从0收集"功能完全不依赖模型，安全可靠
3. **风险提示** - 明确警告用户在模型缺失时使用"自动获取+投资"的风险
4. **数据质量** - 收集的纯原始数据可用于训练高质量模型

**建议工作流程**:
1. 使用"从0开始收集数据并训练"收集初始数据（500+条）
2. 自动训练出适配60怪物的新模型
3. 之后可以安全使用"自动获取数据"（带投资）功能
