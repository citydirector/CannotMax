# 自动数据收集和训练功能使用指南

## 新功能说明

新增了 **"从0开始收集数据并训练"** 按钮，实现一键完成以下流程：

1. **忽略旧模型** - 自动备份当前的错误ONNX模型
2. **自动游戏** - 固定选左，收集对战数据
3. **定时停止** - 到达设定时长后自动停止
4. **自动训练** - 立即开始训练新模型
5. **加载新模型** - 训练完成后自动加载

## 使用步骤

### 1. 设置训练时长

在GUI的"训练时长(小时)"输入框中设置 desired 时长：
- 推荐值：1-4小时
- 最小值：0.5小时（30分钟）
- 根据你想收集的数据量调整

### 2. 点击按钮

点击 **"🔄 从0开始收集数据并训练"** 按钮

### 3. 确认对话框

系统会显示确认信息，包括：
- 当前设置的训练时长
- 将要执行的步骤
- 点击"是"开始执行

### 4. 自动运行

程序会自动：
- 启动游戏
- 每局固定选择左侧
- 记录对战数据到 `data/YYYY_MM_DD__HH_MM_SS/arknights.csv`
- 实时显示剩余时间

### 5. 自动训练

数据收集完成后：
- 自动停止游戏
- 统计收集到的数据量
- 启动模型训练（调用 `train_onnx.py`）
- 显示训练进度

### 6. 完成

训练成功后：
- 自动加载新模型
- 更新窗口标题显示新模型名称
- 启用预测功能
- 显示成功消息

## 注意事项

### 数据量建议

| 时长 | 预计对战场次 | 数据质量 |
|------|-------------|---------|
| 0.5小时 | ~15-20场 | 较少，仅测试用 |
| 1小时 | ~30-40场 | 基础可用 |
| 2小时 | ~60-80场 | 推荐使用 |
| 4小时 | ~120-160场 | 高质量 |

### 游戏模式选择

- **单人模式**：速度较快，适合快速收集数据
- **30人模式**：每局时间较长，但数据更丰富

在点击按钮前，通过"游戏模式"下拉菜单选择。

### 中途停止

如需中途停止：
- 再次点击"从0开始收集数据并训练"按钮
- 在确认对话框中选择"是"
- 或直接关闭GUI窗口

### 冲突处理

以下情况无法启动：
- "自动获取数据"正在运行 → 先停止它
- 自动收集流程已在运行 → 点击按钮可停止

## 技术细节

### 固定选左策略

为了简化数据收集，此功能会：
- 禁用投资功能（`is_invest=False`）
- 每局自动选择"本轮观望"
- 确保数据的一致性

### 模型备份

旧的ONNX模型会被重命名为：
```
models/best_model_full.onnx.backup
```

如果新训练失败，可以手动恢复：
```bash
mv models/best_model_full.onnx.backup models/best_model_full.onnx
```

### 数据存储

所有收集的数据保存在：
```
data/YYYY_MM_DD__HH_MM_SS/arknights.csv
```

文件名包含启动时间戳，便于追溯。

## 故障排除

### Q: 训练失败怎么办？
A: 检查以下几点：
1. 数据量是否太少（至少需要10条以上）
2. 数据格式是否正确（查看CSV文件）
3. 查看日志文件中的错误信息

### Q: 新模型预测不准？
A: 这是正常的，原因可能是：
1. 数据量不足，继续收集更多数据
2. 数据多样性不够，尝试不同的对战组合
3. 重新训练，增加epochs或调整参数

### Q: 想使用之前的数据一起训练？
A: 可以使用"数据打包"功能整理历史数据，然后手动运行：
```bash
uv run python train_onnx.py
```

这会聚合所有 `data/*/arknights.csv` 文件进行训练。

## 完整工作流程示例

```
早上9:00
├─ 设置训练时长: 2小时
├─ 选择游戏模式: 单人
├─ 点击"从0开始收集数据并训练"
├─ 确认开始
│
├─ 9:00-11:00 自动游戏收集数据
│  ├─ 预计收集60-80场对战
│  └─ 数据保存到 data/2026_04_26__09_00_00/
│
├─ 11:00 自动停止游戏
├─ 11:00-11:30 自动训练模型
│  ├─ 聚合数据
│  ├─ 训练PyTorch模型
│  └─ 转换为ONNX格式
│
└─ 11:30 完成！新模型就绪
   └─ 可以开始使用新模型进行预测
```

## 与其他功能的对比

| 功能 | 用途 | 适用场景 |
|------|------|---------|
| **自动获取数据** | 手动控制，可选择投资策略 | 日常数据收集，需要使用预测功能 |
| **从0开始收集数据并训练** | 全自动，固定选左 | 快速建立新模型，重新训练 |
| **训练ONNX模型** | 手动触发训练 | 已有足够数据，只想训练模型 |
| **数据打包** | 整理和归档数据 | 分享数据或清理空间 |
