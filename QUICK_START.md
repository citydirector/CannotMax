# 快速开始：收集训练数据

## 当前问题

你的ONNX模型是用77种怪物训练的，但现在只有60种怪物，导致维度不匹配。

**解决方案**: 收集新的60怪物配置的数据，然后重新训练模型。

## 立即开始

### 步骤1: 启动数据收集

1. 运行你的GUI程序
2. 点击"自动获取数据"按钮
3. 让程序正常运行游戏对战
4. 数据会自动保存到 `data/YYYY_MM_DD__HH_MM_SS/arknights.csv`

### 步骤2: 检查数据进度

随时运行以下命令查看收集进度：

```bash
uv run python tools/prepare_training_data.py
```

这会显示：
- 当前收集了多少条数据
- 哪些怪物出现得最多
- 胜负分布情况
- 是否达到训练要求

### 步骤3: 训练模型

当数据量达到 **500条以上** 时，运行：

```bash
uv run python train_onnx.py
```

这会自动：
1. 合并所有数据
2. 训练新模型（适配60种怪物）
3. 转换为ONNX格式
4. 保存到 `models/best_model_full.onnx`

### 步骤4: 使用新模型

重启GUI程序，新模型会自动加载并开始工作！

## 时间估算

| 对战速度 | 达到100条 | 达到500条 | 达到1000条 |
|----------|-----------|-----------|------------|
| 每局2分钟 | ~3.5小时 | ~17小时 | ~33小时 |
| 每局3分钟 | ~5小时 | ~25小时 | ~50小时 |

**建议**: 可以挂机 overnight 收集数据

## 临时方案（如果需要立即使用）

如果你现在就需要使用预测功能，可以生成测试数据先跑通流程：

```bash
# 生成200条合成数据（仅用于测试流程）
uv run python tools/generate_synthetic_data.py

# 用合成数据训练（预测效果会很差！）
uv run python train_onnx.py
```

**重要**: 合成数据训练的模型预测能力很差，必须用真实数据重新训练！

## 文件说明

- `tools/prepare_training_data.py` - 检查和准备训练数据
- `tools/generate_synthetic_data.py` - 生成测试用的合成数据
- `tools/TRAINING_GUIDE.md` - 详细的训练指南
- `train_onnx.py` - 一键训练脚本（在根目录）

## 需要帮助？

如果遇到问题，检查：
1. `data/` 目录下是否有 `arknights.csv` 文件
2. 文件格式是否正确（应该有122列：60L + 60R + Result + ImgPath）
3. 日志文件中是否有错误信息
